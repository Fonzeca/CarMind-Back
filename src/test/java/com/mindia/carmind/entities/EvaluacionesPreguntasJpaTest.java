/*
 * Created on 2021-10-19 ( 12:45:36 )
 * Generated by Telosys ( http://www.telosys.org/ ) version 3.3.0
 */
package com.mindia.carmind.entities;

import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.junit.jupiter.api.Assertions.assertNull;

import com.mindia.carmind.jpa.tools.JpaTest;

import org.junit.jupiter.api.Test;

public class EvaluacionesPreguntasJpaTest extends JpaTest {
	
    @Override
    public void initData() {
		// DatabaseData.init(em) has been called before
		// Initialize here other data required for the current test
    }
	
    private EvaluacionesPreguntas createEntity() {
    	EvaluacionesPreguntas entity = new EvaluacionesPreguntas();
		entity.setId( Integer.valueOf(200) ) ;
		entity.setEvaluacion( Integer.valueOf(100) ) ;
		entity.setPregunta( Integer.valueOf(100) ) ;
    	return entity;
    }

    private Object getEntityKey(EvaluacionesPreguntas entity) {
    	return entity.getId();
    }

    @Test
    public void testPersistFindRemove() {
    	
    	EvaluacionesPreguntas entity = createEntity();
    	Object key = getEntityKey(entity);

    	// if entity doesn't exist yet
    	if ( find(EvaluacionesPreguntas.class, key) == null ) {

	    	// Create with 'persist'
    		persistAndRefresh(entity);
    	
			// Find
			EvaluacionesPreguntas entity2 = find(EvaluacionesPreguntas.class, key);
			assertNotNull(entity2);
        
			// Remove
			remove(entity2);
			assertNull(find(EvaluacionesPreguntas.class, key));
		}
    }
    
    @Test
    public void testMergeFindRemove() {
    	
    	EvaluacionesPreguntas entity = createEntity();
    	Object key = getEntityKey(entity);

    	// Create or update with 'merge'
    	mergeAndRefresh(entity);
    	
    	// Find
    	EvaluacionesPreguntas entity2 = find(EvaluacionesPreguntas.class, key);
        assertNotNull(entity2);
        
    	// Remove
    	remove(entity2);
        assertNull(find(EvaluacionesPreguntas.class, key));
    }

}
