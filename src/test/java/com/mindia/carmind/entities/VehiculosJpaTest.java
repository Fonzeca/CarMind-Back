/*
 * Created on 2021-10-19 ( 12:45:36 )
 * Generated by Telosys ( http://www.telosys.org/ ) version 3.3.0
 */
package com.mindia.carmind.entities;

import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.junit.jupiter.api.Assertions.assertNull;

import com.mindia.carmind.jpa.tools.JpaTest;

import org.junit.jupiter.api.Test;

public class VehiculosJpaTest extends JpaTest {
	
    @Override
    public void initData() {
		// DatabaseData.init(em) has been called before
		// Initialize here other data required for the current test
    }
	
    private Vehiculos createEntity() {
    	Vehiculos entity = new Vehiculos();
		entity.setId( Integer.valueOf(200) ) ;
		entity.setMarca( "BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB" ) ;
		entity.setModelo( "BBBB" ) ;
		entity.setLinea( "BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB" ) ;
		entity.setFechaService( java.sql.Date.valueOf("2002-06-22") ) ;
		entity.setUltimaEvaluacion( Integer.valueOf(100) ) ;
		entity.setColor( "BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB" ) ;
    	return entity;
    }

    private Object getEntityKey(Vehiculos entity) {
    	return entity.getId();
    }

    @Test
    public void testPersistFindRemove() {
    	
    	Vehiculos entity = createEntity();
    	Object key = getEntityKey(entity);

    	// if entity doesn't exist yet
    	if ( find(Vehiculos.class, key) == null ) {

	    	// Create with 'persist'
    		persistAndRefresh(entity);
    	
			// Find
			Vehiculos entity2 = find(Vehiculos.class, key);
			assertNotNull(entity2);
        
			// Remove
			remove(entity2);
			assertNull(find(Vehiculos.class, key));
		}
    }
    
    @Test
    public void testMergeFindRemove() {
    	
    	Vehiculos entity = createEntity();
    	Object key = getEntityKey(entity);

    	// Create or update with 'merge'
    	mergeAndRefresh(entity);
    	
    	// Find
    	Vehiculos entity2 = find(Vehiculos.class, key);
        assertNotNull(entity2);
        
    	// Remove
    	remove(entity2);
        assertNull(find(Vehiculos.class, key));
    }

}
